## Исправление LIGHT EventLog и factory reset LED WiFi

### LIGHT EventLog

- Убрано визуальное отображение поля `T3` в snapshot журнала профиля `LIGHT`.
- В `LIGHT` snapshot отображает только `T1`, `T2` и `dT`.
- Runtime-защита удалённых сенсоров сохранена.
- В `FULL` отображение `T3` сохранено.

### Factory reset LED WiFi

- Настройка `wifi/led_off` добавлена в применение заводских defaults.
- После factory reset значение `wifi/led_off` сбрасывается в `false`.
- Runtime-состояние `wifiLedOff` синхронизируется с factory default.
- Сохранена текущая GPIO-полярность LED WiFi.
- Поведение relay LED, alarm LED, CH5 buzzer и FULL bell не изменялось.

### Версия

- Было: `1.7.17`
- Стало: `1.7.18`

### Проверки

- FULL-сборка с `-DACTIVE_PROFILE=PROFILE_FULL`: успешно, 1530267 bytes (77%), RAM 59856 bytes (18%).
- LIGHT-сборка с `-DACTIVE_PROFILE=PROFILE_ECONOMY_LIGHT`: успешно, 1518851 bytes (77%), RAM 59752 bytes (18%).
- Embedded UI сгенерирован штатным генератором; `generate_embedded_web.py all --check` прошёл.
- `git diff --check` и проверка синтаксиса `WEB_SRC/page-app.js` прошли.
- `python -m unittest discover -v`: `Ran 0 tests`, `NO TESTS RAN`, код выхода `5`; тестовая инфраструктура отсутствует.
- Аппаратный factory-reset прогон не выполнен: ESP32 недоступен.
